import Slider from 'react-slick';

function InitialConfig() {
  var settings = {
    dots: true,
    infinite: true,
    speed: 500,
    slidesToShow: 1,
    slidesToScroll: 1
  };
  return <>hola</>;
}

export default InitialConfig;
